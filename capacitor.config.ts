import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Esri_Career_Planning',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
